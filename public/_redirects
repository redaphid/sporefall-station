# Short link to the self-hosted APK. The real file is served statically from
# dist/download/backseat.apk (bundled by the deploy workflow) — a real file wins
# over the SPA catch-all below, so these just give it friendly URLs. We serve the
# APK from OUR site (not GitHub) because the repo is private and its release
# assets are auth-walled.
/download      /download/backseat.apk  302
/download/     /download/backseat.apk  302
/get           /download/backseat.apk  302

# SPA fallback: serve index.html for any route so deep links / refreshes work.
# Copied into dist/ by Vite at build time.
/*  /index.html  200
